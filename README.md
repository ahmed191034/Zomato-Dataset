# Zomato Case Study

A case study repo for the Zomato restaurant dataset (ratings, cuisines, cost, location analysis).

## Status

⚠️ The repository currently contains only `Zomato Case Study (1).zip` — no visible notebook, script, or README content. GitHub can't render what's inside a zip, so as it stands this repo shows nothing to a visitor.

## What To Do

1. Extract the zip and commit the actual notebook/script and dataset (or a data-download step) directly to the repo.
2. Add a README describing the business questions explored (e.g. which cuisines/locations get the best ratings, cost vs. rating relationship) and key findings.
3. Rename the zip file locally after extraction — the `(1)` suffix suggests a duplicate download that was never cleaned up.
